# Enum to String Mapper (ETS)

A command-line tool scans a specific file for enums and generates a mapped array for convenient string representation of the enum.

## Purpose

ETS makes it so you don't need to create the array map to the enum manually.

## Output

This scans the `item_enum` file and generates an Odin file with:

```odin
No clue the codes not done yet, if youd consult main.go
```